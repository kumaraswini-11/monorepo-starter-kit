# Claude Code

Claude Code reads `CLAUDE.md`, not `AGENTS.md`. To keep a **single source of
truth** for every agent (Claude Code, Cursor, Codex, Copilot, …), this file just
imports the shared, tool-agnostic `AGENTS.md` below. Put Claude-Code-specific
notes (e.g. permission hints, plan-mode preferences) under the import if any ever
arise.

@AGENTS.md

# storybook

Storybook for the `@workspace/ui` design system — a dev workbench plus interaction + a11y tests
(Vitest browser mode).

## Scripts

`storybook` (dev) · `build:storybook` · `test` (Vitest browser)

See ADR [0018](../../docs/decisions/0018-storybook-and-visual-testing.md).

// Node-environment unit tests for this package (ADR 0029). Shared preset lives in
// @workspace/vitest-config; co-located `src/**/*.test.ts`.
export { base as default } from "@workspace/vitest-config/base";

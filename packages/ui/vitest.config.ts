// Component tests for @workspace/ui — jsdom + Testing Library (ADR 0025). Shared preset in
// @workspace/vitest-config; co-located `src/**/*.test.{ts,tsx}`.
export { dom as default } from "@workspace/vitest-config";
